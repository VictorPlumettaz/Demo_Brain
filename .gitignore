# Private folder stays local — but keep the README so the structure is visible
07_private/*
!07_private/README.md

# Obsidian workspace state (per-device, not worth syncing)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Plugin CODE is not ours to redistribute — but which plugins, which versions and
# which settings absolutely belong in the repo. So: manifest.json and data.json stay,
# the bundles do not. Install them yourself, see README.
.obsidian/plugins/*/main.js
.obsidian/plugins/*/styles.css
.obsidian/plugins/*/*.wasm
.obsidian/plugins/*/brat-migrations.json

# OS clutter
.DS_Store
Thumbs.db
desktop.ini

# Binaries do not belong in a text repo
*.exe
*.dll
*.msi
*.zip
node_modules/

# Audio: transcribe it, then delete it
*.m4a
*.mp3
*.wav
