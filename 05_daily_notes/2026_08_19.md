---
title: 2026-08-19 (Wednesday)
tags: [daily, kw34]
date: 2026-08-19
created: 2026-08-19
updated: 2026-08-19
---

# 2026-08-19 — Wednesday

## What happened

Go-live date confirmed. Encoding fix in and the customer data re-imported clean. And the coffee
machine accepts writes.

## Done

- [[glaswerk_nord_overview]]: go-live confirmed for **Monday 5 October**. Cutover over the weekend
  of 3/4 October, then two weeks of parallel running against their old system. [[lena_mullion]]
  has it in writing from their operations manager. Deploy freeze on [[paneflow]] from 2 to
  6 October.
- Import profiles now carry an explicit encoding per file. Default is Windows-1252 for Glasplan
  exports. After decoding, the importer scans for `Ã` sequences and rejects the file instead of
  importing it quietly → [[glaswerk_nord_tasks]]
- Re-imported all nine files. 1,143 previously broken rows are correct. Spot-checked the ten longest
  product descriptions by hand.
- Coffee machine: `POST /api/v1/config` with `{"grind": 6}` changes the grind setting. No
  authentication, no token, no rate limit. I moved it from 5 to 6, confirmed the change through
  `GET /api/v1/status`, and set it back to 5 → [[coffee_machine_api_findings]]
- The same endpoint shape exists for `POST /api/v1/service/descale`. I did not send that one.
- Reported all of it to [[nils_putty]]. He was delighted for roughly ninety seconds — he has been
  reading the bean level off the display for two years and had no idea there was an API — and then he
  stopped being delighted, because the machine sits on the office VLAN, has no authentication, and
  takes writes.
- Nils opened a ticket with the vendor and is moving the machine onto the IoT VLAN this week.

## Open

- [ ] #priority/high Document every endpoint before the machine moves — Robin, [[coffee_machine_api_tasks]]
- [ ] #priority/medium Merge PR #212 once [[marco_bevel]] signs off — Robin
- [ ] #priority/high Book the cutover dry run for the first week of September — Robin, [[glaswerk_nord_tasks]]
